# Maze Solver

This program creates and solves a random grid-based maze.

## Requirements
Python >= 3.9

## Usage
From the root directory run:
`python main.py`

## Features
THis was a guided project from boot.dev
This program creates a grid-based maze, then uses a depth-first search to solve the maze. The maze and solution are displayed in a Tkinter GUI.
